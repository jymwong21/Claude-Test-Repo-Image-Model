# ── Flux LoRA Studio — full GPU environment (training + inference) ────────────
# Requires `git` at install time (diffusers is pinned to a commit, below).
#
# torch is intentionally NOT pinned here. Install the CUDA 12.8 (cu128) build of
# torch FIRST — runpod/setup.sh does this, or use a runpod/pytorch *-cu1281-torch280-*
# image. Pinning torch in this file would pull a CPU/wrong-CUDA wheel that cannot
# run on the RTX 5090 (Blackwell, sm_120).
#
# The ML versions below MIRROR ostris/ai-toolkit (its requirements_base.txt) so
# that training (ai-toolkit) and inference (diffusers) share ONE consistent set.
# If you've already run runpod/setup.sh, installing this file is a no-op for the
# ML libraries and just layers on the web server. Verified to co-resolve via
# `pip install --dry-run`:
#   transformers 5.5.3 · huggingface_hub 1.10.1 · tokenizers 0.22.2 · peft 0.18.1
#   · optimum-quanto 0.2.4 · accelerate 1.13.0 · safetensors 0.7.0

# Web backend + app
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
aiofiles>=24.1
pydantic>=2.7
python-dotenv>=1.0
pyyaml>=6.0
Pillow>=10.4

# ML stack — pinned to match ai-toolkit (transformers 5.x / huggingface_hub 1.x).
# transformers 5.5.3 also provides NATIVE Florence-2 (no trust_remote_code).
transformers==5.5.3
huggingface_hub==1.10.1
peft==0.18.1
optimum-quanto==0.2.4
accelerate>=1.1.0
safetensors>=0.4.3
# diffusers: the SAME commit ai-toolkit pins (Flux + transformers-5 compatible).
# Released diffusers <0.38 lacks transformers-5 runtime support; 0.38 demands an
# unreleased safetensors 0.8 — so we mirror ai-toolkit's commit for one coherent env.
diffusers @ git+https://github.com/huggingface/diffusers.git@dc8d9032171c83741fd37ed2b12bc9d8274464f3

# Flux T5 tokenizer + native Florence-2 captioner (vision tower) extras
sentencepiece>=0.1.99
protobuf>=3.20.3
einops>=0.8
timm>=1.0.23
