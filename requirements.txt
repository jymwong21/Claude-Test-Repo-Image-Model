# ── Flux LoRA Studio — application dependencies ───────────────────────────────
# NOTE: torch is intentionally NOT pinned here. The RTX 5090 (Blackwell, sm_120)
# requires a CUDA 12.8 build of PyTorch installed separately — see runpod/setup.sh.
# Installing torch from this file would pull a CPU/old-CUDA wheel that can't run
# on Blackwell. Always run runpod/setup.sh first.

# Web backend
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
aiofiles>=24.1
pydantic>=2.7
python-dotenv>=1.0

# Inference (diffusers stack). Versions chosen for Flux + Blackwell support.
diffusers>=0.32
transformers>=4.47
accelerate>=1.2
safetensors>=0.4.5
sentencepiece>=0.2
protobuf>=4.25
peft>=0.14
Pillow>=10.4

# Auto-captioning (Florence-2 runs on transformers). einops is required by it.
einops>=0.8
timm>=1.0
